# range

```
tip> range
usage: set v = range(N,x0,x1)
  set the data v which size N, start from x0 to the end x1.
  data increment dx = (x1-x0)/(N-1)
  ex.) range(5,0,2) set the data {0, 0.5, 1, 1.5, 2}.
```

